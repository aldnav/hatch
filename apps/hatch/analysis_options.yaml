include: package:very_good_analysis/analysis_options.yaml

analyzer:
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'

linter:
  rules:
    # App code is not a published library — doc comments on every widget
    # class add noise without value.
    public_member_api_docs: false
